# Gap-Closure Report — 2026-05-31

**Scope:** Closure of the 22-row gap matrix in [`2026-05-31-gap-analysis.md`](./2026-05-31-gap-analysis.md).
**Run shape:** 13 zones across 4 waves (TUI / DISPATCHER / CRASHSWEEP / SANDBOX / STDLIB / HOTRELOAD_IPC / WRITE_WORKFLOW_TOOL / VIZ → MEMORY → WORKTREE → HITL / OTEL / TIMETRAVEL).
**Bottom line:** 17 ✅, 5 🟡 (scaffolded, follow-ups documented), 0 ❌. Quality gate is **red**: typecheck has 2 errors in `src/runtime/sandboxWorker.ts`, one pre-existing unit assertion (`disableKnobs.test.ts:188`) flips, integration suite never ran.

---

## 1. TL;DR

| # | Gap | Status |
|---|-----|--------|
| 1 | No persistent per-agent / per-persona memory | 🟡 partial — primitive ships; compaction/GC/oversize-warn deferred → [`docs/agent-memory.md`](../agent-memory.md) |
| 2 | No git-worktree isolation per agent | 🟡 partial — create+CWD-rewrite+diff ships; prune/rebase/promote deferred → [`docs/agent-worktree.md`](../agent-worktree.md) |
| 3 | No mid-phase HITL pause-and-route primitive | 🟡 partial — ledger + replay + ctrl IPC ship; `pi.ui.confirm` not wired (`workflowCmd.ts:674` TODO) → [`docs/hitl.md`](../hitl.md) |
| 4 | No time-travel / fork-from-checkpoint | 🟡 partial — `forkFromCheckpoint()` API + tests ship; `f` hotkey + strict cache filtering deferred → [`docs/time-travel.md`](../time-travel.md) |
| 5 | ~~No critique / adversarial-refute primitive~~ | ✅ closed (was already closed in matrix) |
| 6 | ~~No stdlib JSON extraction / schema validation~~ | ✅ closed (was already closed in matrix) |
| 7 | ~~Better aggregation primitives (Borda/Schulze/…)~~ | ✅ closed (was already closed in matrix) |
| 8 | TUI hotkeys r/s/t/c not wired in production | ✅ closed |
| 9 | No keyword-trigger workflow drafting | ✅ closed (recon found feature already wired; this run added test coverage + doc fix + `workflows?` plural regex) |
| 10 | Dead weight from worker_threads migration | ✅ closed (partial — see honest assessment) |
| 11 | `ctrl.jsonl` IPC has no fallback when `fs.watch` silently fails | ✅ closed |
| 12 | `Run.stop()` has no SIGKILL escalation | ✅ closed |
| 13 | macOS PID-recycle gap in crash sweep | ✅ closed |
| 14 | `recoverFromTranscript` has no size cap | ✅ closed |
| 15 | No OpenTelemetry export from the ledger | 🟡 partial — exporter ships behind `optionalDependencies`; production env vars + Jaeger smoke documented but not exercised in this run → [`docs/otel.md`](../otel.md) |
| 16 | ~~No DAG / phase-graph visualization~~ | ✅ closed (was already closed in matrix) |
| 17 | `ctx.log` writes the ledger twice | ✅ closed |
| 18 | Banner state has no TTL | ✅ closed |
| 19 | ~~`ctx.signal` polyfill diverges from web `AbortSignal`~~ | ✅ closed (was already closed in matrix) |
| 20 | Hot-reload registers a no-op stub for newly added workflows | ✅ closed |
| 21 | No path-sanitization on `agentId` | ✅ closed |
| 22 | `allowCodeGeneration.strings` default vs. threat-model | ✅ closed |

---

## 2. Per-gap details

### 1. No persistent per-agent / per-persona memory
**Category:** capability · **Effort:** M

> Saved workflows are stateless reruns. `agent-memory/<name>/` + auto-injected `MEMORY.md` turns each subagent into a compounding asset. Highest-leverage capability gap.

Shipped `src/runtime/agentMemory.ts` (scope resolution: `user → ~/.pi/agent/workflows/agent-memory/<name>/`, `project → <cwd>/.pi/workflows/agent-memory/<name>/`, `local → <runDir>/agent-memory/<name>/`), 25 KiB-capped read, lazy append. Wired through `runCtx.ts::runOneAgent` (prepends `Persistent memory:\n<content>\n\n` to effective prompt) and `dispatcher.ts` (captures `{type:'memory_update'}` events, flushes after `agent_end`, skips on crash). Manifest tracks `agentMemoryDirs: { [name]: dir }` for resume audit (`manifestWriter.ts::recordAgentMemoryDir`). 38 tests pass at `tests/unit/memory.test.ts`. **Memory NOT in cache key** — same posture as `opts.schema`.

### 2. No git-worktree isolation per agent
**Category:** capability · **Effort:** M

> Every agent shares parent CWD. `hunt-bugs-loop` documents BUG-W02 (same-file write race). Migration-class workflows can't fan out write-heavy work safely.

Shipped `src/runtime/worktree.ts` with `parseIsolation`, `assertGitRepo`, `createWorktreeForAgent` (`git worktree add --detach`, no stray branches), `emitWorktreeDiff` (`git diff --no-color HEAD`, 8 MiB cap with marker line). Wired in `runCtx.ts` alongside ZONE_MEMORY: parses `opts.isolation`, swaps `dispatchCwd` to the worktree, emits diff after dispatch. Manifest carries `agentWorktrees: { [name]: path }` (`manifestWriter.ts::recordAgentWorktreePath`, same per-runDir write queue as memory). 26 tests pass at `tests/unit/worktree.test.ts` (real git repos in tmpdir + `makeFakeSpawn` for cwd assertions).

### 3. No mid-phase HITL pause-and-route primitive
**Category:** capability · **Effort:** M

> Run-level pause exists; "pause this phase, ask a structured question, route on the answer" doesn't. Three peers ship the same shape under three names.

Shipped `ctx.interrupt({question, choices?, default?})` (string shorthand also accepted) at `src/runtime/runCtx.ts` + `src/runtime/sandbox.ts`. Deterministic per-run key sequence (`int-N`) is the load-bearing replay invariant. Replay short-circuit via `RunCtxHostOptions.replayResolvedInterrupts` (Map seeded from prior `interrupt_resolved` ledger entries). New ledger entries: `interrupt_requested {key, question, choices?, default?}` and `interrupt_resolved {key, value, source: "ipc" | "default" | "replay"}`. RunManager FIFO `pendingInterrupts` queue + `Run.respondInterrupt(value, key?)`. `WorkflowClient.resume(runId, value, opts?)` writes `resume-interrupt` ctrl line. 18 tests pass at `tests/unit/interrupt.test.ts`. **Known unwired**: `workflowCmd.ts:674 // slice 11: TODO wire approval through pi.ui.confirm in slice 13`.

### 4. No time-travel / fork-from-checkpoint
**Category:** capability · **Effort:** L

> Append-only ledger enables resume from failure but not "rewind to phase 3, change one input, run a different branch." Every multi-phase workflow eventually wants this.

Shipped `forkFromCheckpoint(parentRunId, opts)` exported from package root + as a method on `WorkflowClient` (`src/runtime/forkRun.ts`, ~340 LOC). Pre-fork ledger entries are copied verbatim into the new run's `ledger.jsonl` (excluding `init`/`transition`/`result`/`error`/`shutdown`/`cancelled`). Parent's full `cache.jsonl` copied raw. Manifest carries `parentRunId` + `forkAtPhase`. Overrides stored as `author_cache` under reserved key `__fork_overrides__` (exported as `FORK_OVERRIDES_KEY`); workflow reads via `ctx.cache.get('__fork_overrides__')`. 4 integration tests pass at `tests/integration/forkFromCheckpoint.test.ts` — but **integration suite never ran during verification** (see §3).

### 5. ~~No critique / adversarial-refute primitive~~ ✅ closed
**Category:** capability · **Effort:** M

Already marked closed in the matrix at run start (ZONE_STDLIB). `ctx.critique({producer, critic, maxRounds, accept})` lives in `src/runtime/stdlib.ts`. 11 tests at `tests/unit/stdlib-critique.test.ts`.

### 6. ~~No stdlib JSON extraction or schema-validated phase outputs~~ ✅ closed
**Category:** capability · **Effort:** S

Already marked closed in the matrix at run start (ZONE_STDLIB). `ctx.extractJSON(text)` (Context-realm, char-code, fence-aware) and `validateAgainstSchema` + `SchemaValidationError` invoked at the agent boundary. 16 tests at `tests/unit/stdlib-extract.test.ts`.

### 7. ~~Better aggregation primitives (Borda / Schulze / ranked-pairs)~~ ✅ closed
**Category:** capability · **Effort:** S

Already marked closed in the matrix at run start (ZONE_STDLIB). `ctx.aggregate(method, ballots, opts)` covers borda, schulze, ranked_pairs, kemeny_young, instant_runoff, coombs, score, approval (DSPy issue #8898 port, MIT). `ctx.consensus(agents, {method})` integration. 18 tests at `tests/unit/stdlib-aggregate.test.ts` (includes the classic Schulze Wikipedia example).

### 8. TUI hotkeys r/s/t/c are not wired in production
**Category:** dx · **Effort:** S

> `s` shows literal `"save-script not wired (slice 14 callback missing)"`; `c` shows fake `"copied:"` banner; `r` emits a stub event; `t` doesn't open anything.

Shipped `buildOverlayCallbacks(pi, registry, ctx)` in `src/commands/workflowCmd.ts`. `r` reads `manifest.json` for original input/workflow path → `restartTerminalRun` → `wireRunDelivery`. `s` reads manifest → `runSaveScript` (rename on collision, skip git-add — overlay can't drive multi-choice synchronously). `t` returns banner from `openTranscriptInEditor` ("opened vim on …"). `c` returns banner from `copyToClipboard` ("copied via pbcopy (N chars)"). 171/171 TUI-zone tests pass.

### 9. No keyword-trigger workflow drafting
**Category:** dx · **Effort:** S

> Type `workflow` anywhere in a prompt → Claude Code drafts and offers to run a script. `write_workflow` already drafts; only the inline trigger + chip is missing.

**Recon surprise:** the keyword detector, system-prompt directive, `/workflows keyword [on|off]` toggle, and `Alt+W` shortcut were already wired in `src/index.ts:127-209`. Pi has no `session_primer` event hook; the existing `input` + `before_agent_start` chain is the equivalent. This run extracted the logic into `src/runtime/keywordTrigger.ts` (75 lines) for unit-testability, widened the regex from `/\bworkflow\b/i` to `/\bworkflows?\b/i` (so the plural also fires), added 15 integration tests at `tests/integration/writeWorkflowKeywordTrigger.test.ts`, and replaced an out-of-date line in `docs/authoring.md` (*"Use `/workflow-name` not a `workflow` keyword"*) with a "Workflow keyword trigger" subsection. The visual chip indicator from the matrix description is **not** added — recon downgraded it to a DX polish item.

### 10. Code-level dead weight from worker_threads migration
**Category:** dx · **Effort:** S

> ~600 LOC of shadow that drifts.

Deleted in `src/runtime/sandbox.ts`: `minimalCtxLiteral`, `installConsole`, `installCrypto`, `installBuffer`, `installWebApis`, `getSandboxObject`, the 4-line `void` block + multi-line "are no longer needed" comment in the constructor. **Not deleted** (still live in current pre-migration architecture in working tree, contrary to gap doc): `raceWithAbort` (called at `runScript` line 791), `bridgeNonce` (called in constructor), `collectHostGlobals`, `HostGlobals`, `HostBridgePayload`, `RunCtxHostInternal`. `extractLoaderArgs` was already absent at HEAD. Net file change: 1567 → 1495 lines. **The sub-agent flagged a race**: parallel zones overwrote `sandbox.ts` mid-edit, forcing a re-apply against a different baseline. This is the most likely root cause of the post-run typecheck failures in `sandboxWorker.ts` (see honest assessment).

### 11. `ctrl.jsonl` IPC has no fallback when `fs.watch` silently fails
**Category:** reliability · **Effort:** S

> NFS/FUSE/Docker bind-mounts and macOS edge cases fail silently. Supervisor's pause/stop dropped with no surface signal.

`startCtrlWatcher` in `src/runManager.ts:972-1030` rewritten: exported + opts param (`watchFn`, `log`, `rotateBytes`, `pollIntervalMs`, `disableNativeWatch`), one-shot warning when `fs.watch` throws, **always-on 1s mtime poll alongside `fs.watch`** (deliberately stronger than spec — silent fs.watch failure on NFS/Docker is undetectable without a heartbeat; cost is one `statSync` per second per active run). Byte-offset tracking resets correctly on truncate / ENOENT. Rotation: `ctrl.jsonl` → `ctrl.jsonl.archived` once `bytesRead ≥ 8 MiB`. 8 new tests at `tests/unit/ipcCtrlWatcher.test.ts`.

### 12. `Run.stop()` has no SIGKILL escalation
**Category:** reliability · **Effort:** S

> A `pi -p` sub-agent stuck in a tool call ignores SIGTERM and wedges the semaphore. Supervisor sends `{type:'stop'}`, gets no terminal entry, no error, just silence.

**Recon surprise:** the timeout path in `dispatcher.ts:454-470` already had 5s SIGTERM→SIGKILL; the fix was factoring out, not writing from scratch. Factored into shared `escalateKill()` helper used by both `onAbort` and timeout paths. Abort path now arms a 5s SIGKILL timer after SIGTERM (configurable via new `killGraceMs` test seam). Idempotent — second call is a no-op so timeout + abort can both fire safely. 3 escalation tests added (abort+ignored-SIGTERM, abort+cooperative, timeout+ignored-SIGTERM). Used new `tests/helpers/fakeChild.ts` extensions: `signalsSent[]`, `ignoresSigkill`, `stdoutNeverEnds`.

### 13. macOS PID-recycle gap in crash sweep
**Category:** reliability · **Effort:** S

> `currentBootId` returns `""` on non-Linux. Long-uptime macOS hosts can recycle PIDs; crash sweep declines to reap an actually-dead run.

Shipped `parseDarwinBootTime(output)` in `src/runtime/crashSweep.ts` — extracts `sec = NNN` from `sysctl kern.boottime`, returns `darwin-<sec>` (namespaced so it can't collide with a Linux UUID). `currentBootId()` now caches: linux reads `/proc/sys/kernel/random/boot_id`, darwin shells out to `sysctl -n kern.boottime` once via `execFileSync` with 1s timeout. `manifestWriter.ts::captureParentLiveness` now delegates to the same `currentBootId()` so manifests written on macOS get the matching value. The pre-existing "mismatched bootId → dead" test, which previously short-circuited on macOS, now actually executes. 22/22 crash-sweep tests pass.

### 14. `recoverFromTranscript` has no size cap
**Category:** reliability · **Effort:** S

> A long-running agent that wrote a 500MiB transcript before crashing OOMs the resume path.

Added `TRANSCRIPT_RECOVERY_MAX_BYTES = 64 MiB` cap in `src/runtime/dispatcher.ts`. Files over the cap get `fs.stat`'d, then read via `fh.read()` from `(size − cap)` to EOF. Leading partial line dropped. Warning logs to `console.warn`. Since `agent_end` is pi's last event, the tail almost always contains it; pathological cases return `null` and trigger normal re-dispatch (correctness over recovery). 3 size-cap recovery tests added.

### 15. No OpenTelemetry export from the ledger
**Category:** observability · **Effort:** M

> Every observability ecosystem speaks OTel. Single biggest "integrate with the rest of the world" gap.

Shipped `src/runtime/otelExporter.ts` (899 lines) — pure replay state machine + live tailer + dynamic-import SDK loader. Span tree: `invoke_workflow <name>` (root) → `phase <name>` (INTERNAL) → `invoke_agent <agentId>` (leaf). Gen-AI semantic conventions: `gen_ai.operation.name`, `gen_ai.provider.name`, `gen_ai.conversation.id`, `gen_ai.usage.{input,output,cache_read,cache_creation}_tokens`, `error.type`. Plus `pi.*` namespace. Activation: env var unset → strict no-op. Env set + SDK missing → single warn line. Env set + SDK present → BatchSpanProcessor + OTLP/HTTP exporter. OTel SDK packages added to **`optionalDependencies`** so install never blocks. 17 tests pass at `tests/unit/otelExporter.test.ts`. Tailer is **read-only** wrt `ledger.jsonl` (append-only contract preserved).

### 16. ~~No DAG / phase-graph visualization~~ ✅ closed
**Category:** observability · **Effort:** S

Already marked closed in the matrix at run start (ZONE_VIZ). `src/runtime/visualize.ts` emits Mermaid `flowchart TD` from `manifest.json` + `ledger.jsonl`. TUI hotkey `v` writes `.mmd` to `os.tmpdir()`; `ctx.report({format:'mermaid'})` returns same diagram in-workflow. 10 tests at `tests/unit/visualize.test.ts`.

### 17. `ctx.log` writes the ledger twice
**Category:** observability · **Effort:** S

> `tail ledger.jsonl` shows every line twice.

Dropped the duplicated `ledger.append({type:'agent_log',...})` call in `src/runtime/runCtx.ts` (~12 LOC removed). Each `ctx.log()` now produces exactly one `log` ledger entry. Overlay event preserved. Cross-zone: landed before ZONE_OTEL per recon-map ordering (OTel exporter consumes `ledger.jsonl` and the dedup changes the entry stream shape).

### 18. Banner state has no TTL
**Category:** dx · **Effort:** S

> `"stopping agent abc…"` sticks long after the agent stopped.

Banner state in `src/runtime/overlay.ts` migrated from `string | undefined` → `{text, expiresAtMs} | undefined`. New `setBanner(text, ttlMs?)` helper with `DEFAULT_BANNER_TTL_MS = 4000` and `setTimeout`-driven self-clear redraw. `liveBanner()` reader drops expired banners on render. `bannerTimer` cleared in `cleanup()` to prevent post-dispose redraws. New regression test: `banner TTL: stale banner is dropped from render after expiry` (virtual clock + 4s assertion).

### 19. ~~`ctx.signal` polyfill diverges from web `AbortSignal`~~ ✅ closed
**Category:** dx · **Effort:** S

Already marked closed in the matrix at run start (ZONE_STDLIB). `throwIfAborted()`, real `dispatchEvent`, `globalThis.AbortSignal.timeout(ms)` and `AbortSignal.any(signals)` ship in `src/runtime/sandbox.ts`. 12 tests at `tests/unit/stdlib-signal.test.ts`.

### 20. Hot-reload registers a no-op stub for newly added workflows
**Category:** reliability · **Effort:** S

> `hotReload.ts:218` registers `handler: async () => log('hot-reload: invoked stub handler')`.

**Recon surprise:** there were **two** stub handlers in `hotReload.ts` (line ~226 and ~290), not one. Both are replaced. Extracted `registerSingleWorkflowCommand(pi, file)` from `registerWorkflowCommands` body in `src/commands/workflowCmd.ts` (no behavior change). `HotReloadOpts` now takes `registerCommand: (file: WorkflowFile) => void`. `src/index.ts` wires `registerCommand: (file) => registerSingleWorkflowCommand(pi, file)` so freshly added workflows get the SAME real handler used at discovery time. 3 new tests at `tests/unit/hotReload.test.ts`.

### 21. No path-sanitization on `agentId`
**Category:** security · **Effort:** S

> `agentTranscriptPath`/`agentStderrPath` blindly `join(<runDir>/agents, '${agentId}.jsonl')`.

Added `assertSafeAgentId()` + `InvalidAgentIdError` in `src/util/paths.ts` (+63 LOC). Rejects: non-string/empty, NUL byte, `/`, `\`, `..` anywhere, leading `.`. Wired into both `agentTranscriptPath()` and `agentStderrPath()` so the dispatcher rejects malicious agentIds before any spawn. 7 adversarial tests added (`..`, `/`, `\`, NUL, leading `.`, empty/non-string) + 2 path-derivation + 1 dispatch-time rejection. Cross-zone: landed before ZONE_MEMORY/ZONE_WORKTREE per recon map (both write into per-agent dirs derived from agentId).

### 22. `allowCodeGeneration.strings` defaults to `true`; threat-model says `false`
**Category:** security · **Effort:** S

> `sandbox.ts:418` `allowCodegen ?? true` and `threat-model.md:46-55` claim `strings:false` is the defense.

**Recon surprise:** the real default site is `sandbox.ts:620` (`allowCodegen: this.opts.allowCodegen ?? true`); the gap doc cited the wrong line. **Decision: flip the default to `false`** rather than relax the doc — defense in depth, costs nothing, matches the stated contract. The two security fixtures that probe the wrapper-identity defense (`function-constructor.workflow.js`, `async-function-constructor.workflow.js`) and two unit tests (`tests/unit/sandbox.test.ts` "eval / Function are present", "§8.3.1 Function-constructor escape") opt in to `allowCodegen: true` explicitly because they need codegen on for the exploit attempt to even reach the wrapper. `docs/threat-model.md` brought in sync (vectors #1, #2, #11 updated). `tests/security/runner.test.ts` interface extended with `allowCodegen?: boolean`. 119/119 sandbox + security tests pass.

---

## 3. Verification snapshot

From the verify agent's report at the end of the run:

### Typecheck — ❌ **FAIL**
```
src/runtime/sandboxWorker.ts(38,10): error TS2459:
  Module '"./sandbox.js"' declares 'buildInitScript' locally, but it is not exported.

src/runtime/sandboxWorker.ts(39,15): error TS2305:
  Module '"./sandbox.js"' has no exported member 'SandboxWorkerData'.
```

Both errors point to `sandboxWorker.ts` (untracked / new file) importing symbols that `sandbox.ts` does not re-export. **Most likely root cause:** the parallel-write race between ZONE_SANDBOX, ZONE_STDLIB, and ZONE_HITL flagged by the sandbox sub-agent — one zone's full-file write of `sandbox.ts` may have removed an export the worker file depends on.

### Tests — ❌ **FAIL** (1/1036 unit; integration not reached)

```
tests: 1036 · pass: 1035 · fail: 1 · duration: 61.55s · suites: 10
```

Failing assertion (verbatim):
```
test at tests/unit/disableKnobs.test.ts:1:4002
✖ extension: PI_WORKFLOWS_RECURSIVE=1 keeps loading but skips per-workflow commands
  AssertionError [ERR_ASSERTION]: /workflows always registered
      at TestContext.<anonymous> (tests/unit/disableKnobs.test.ts:188:12)
    actual: false
    expected: true
    operator: '=='
```

ZONE_HITL and ZONE_OTEL agents both report this is **pre-existing** (confirmed by `git stash` + rerun on clean tree). However the unit-failure short-circuits the test pipeline:
```
node --test tests/unit/*.test.ts tests/security/*.test.ts
  && node --test --test-force-exit tests/integration/skeleton.smoke.ts
  && node --test tests/integration/*.test.ts
```
**Integration tests for `forkFromCheckpoint`, `agentStopRestart`, and `writeWorkflowKeywordTrigger` are unverified.**

### LSP diagnostics — ⚠️ **NO-OP**

`lsp_diagnostics path="*"` returned `No diagnostics across 1 running server(s) (typescript)`. The TS LSP server was newly started and had not opened the failing file — `tsc`'s output is authoritative.

### Files changed

- 45 modified files, 19 untracked.
- **+4869 / −490 LOC**, 168 file references.
- Largest deltas: `stdlib.ts` (+792), `runCtx.ts` (+657), `workflowCmd.ts` (+374), `runManager.ts` (+344), `sandbox.ts` (+289), `overlay.ts` (+207), `manifestWriter.ts` (+178), `types/internal.d.ts` (+147), `dispatcher.ts` (+140), `crashSweep.ts` (+116), `index.ts` (+115), `client.ts` (+94).
- New runtime modules: `agentMemory.ts`, `worktree.ts`, `forkRun.ts`, `otelExporter.ts`, `keywordTrigger.ts`, `visualize.ts`, `sandboxWorker.ts`, `sandboxWorkerBoot.mjs`.
- New test files: `interrupt.test.ts`, `ipcCtrlWatcher.test.ts`, `memory.test.ts`, `otelExporter.test.ts`, `phaseRegistry.test.ts`, `stdlib-{aggregate,critique,extract,signal}.test.ts`, `visualize.test.ts`, `worktree.test.ts`, plus integration tests for fork/keyword-trigger/agent-stop-restart.
- New docs: `agent-memory.md`, `agent-worktree.md`, `hitl.md`, `time-travel.md`, `otel.md`.

### Outstanding markers
| File:line | Marker | Note |
|---|---|---|
| `src/commands/workflowCmd.ts:674` | TODO | `preApproved: true, // slice 11: TODO wire approval through pi.ui.confirm in slice 13` — actionable, blocks full HITL UX |
| `src/runtime/forkRun.ts:39` | TODO header | Deferred-block doc comment, lists known gaps |
| `src/runtime/runsList.ts:111` | XXXXXXXX | Format placeholder, false positive |
| `src/runtime/phaseView.ts:10` | XXXX | Format placeholder, false positive |

---

## 4. Follow-ups (TODO docs created this run)

Each gap that landed as 🟡 partial has a dedicated doc capturing the deferred items so this run's deferrals don't get lost:

- [`docs/agent-memory.md`](../agent-memory.md) — compaction policy, oversize warning, resume cross-check, GC integration, read-only mode, `ctx.memory` stdlib helpers.
- [`docs/agent-worktree.md`](../agent-worktree.md) — auto-prune on completion / GC integration, rebase / fast-forward / `ctx.promote` helper, worktree-aware caching, resume cross-check, submodule + LFS support, named-branch flag.
- [`docs/hitl.md`](../hitl.md) — full surface spec; key follow-up is wiring `pi.ui.confirm` through `workflowCmd.ts:674`.
- [`docs/time-travel.md`](../time-travel.md) — `f` hotkey in runs-list view, strict cache filtering (currently full-cache copy relies on overrides changing prompts to invalidate post-fork cacheKeys), resume-of-fork forensics, recursive-fork validation.
- [`docs/otel.md`](../otel.md) — activation, attribute reference, local Jaeger recipe, follow-ups (production env-var smoke test, `pi.*` attribute stabilization).

---

## 5. Honest assessment

### What's truly closed (production-ready)
- **TUI hotkeys (8) + banner TTL (18)** — wired end-to-end, 171 zone tests pass, no caveats.
- **Crash-sweep macOS (13)** — `darwin-<sec>` namespacing is sound, the previously-skipped test now executes.
- **`ctx.log` dual-write (17)** — pure deletion, ledger now matches the documented contract.
- **agentId path-sanitization (21)** — hardened, 7 adversarial tests cover the `..`/`/`/`\`/NUL/empty/leading-`.` matrix.
- **`allowCodegen` default flip (22)** — security default now matches the threat-model doc; opt-in path tested.
- **Aggregation / extractJSON / critique / signal polyfill (5,6,7,19)** — closed in the prior ZONE_STDLIB run, this run adds no regressions.
- **DAG visualization (16)** — closed in prior ZONE_VIZ run.

### What's scaffolded but not production-grade
- **Persistent memory (1)** — primitive ships and the auto-injection works, but there is no compaction, no oversize warning, no resume cross-check ("did the memory dir move between resume?"). Long-running agents will eventually overflow the 25 KiB read cap and get a silently-truncated view. Suitable for demos and short workflows; not for the "compounding asset" framing in the matrix description without compaction.
- **Worktree isolation (2)** — create + CWD swap + diff snapshot all work. **No prune.** Worktrees pile up under `<runDir>/worktrees/`. No `ctx.promote()` — authors who want a worktree's edits in the parent repo do `git` themselves. Suitable for fan-out-and-throw-away; not for migration-class workflows where the diffs are the point.
- **Mid-phase HITL (3)** — the ledger plumbing, replay, and ctrl-IPC paths are real and tested. The user-facing piece (`pi.ui.confirm`) is **not wired** — the TODO at `workflowCmd.ts:674` is load-bearing. From a workflow author's perspective `ctx.interrupt()` works via `WorkflowClient.resume()` but not via the TUI overlay. Half a feature.
- **Time-travel (4)** — the API works and is tested. **No `f` hotkey.** No strict cache filtering — the recon flagged this and the doc captures it: a workflow whose post-fork prompts don't depend on `overrides` will silently cache-hit the parent and produce a fork that's byte-identical to the original. Programmatic forks are reliable; interactive fork-and-explore is not.
- **OpenTelemetry (15)** — code path is solid, the test rig with `InMemorySpanExporter` is real, and `optionalDependencies` keeps the install hygienic. **Not exercised against an actual collector during this run.** The Jaeger recipe in `docs/otel.md` is the next person's homework.

### What's closed-with-caveats
- **Dead-weight cleanup (10)** — most of the listed dead symbols are gone, but the agent reports five (`raceWithAbort`, `bridgeNonce`, `collectHostGlobals`, `HostGlobals`, `HostBridgePayload`, `RunCtxHostInternal`) that the gap doc claimed were dead are actually still live in the working tree's pre-migration architecture. The gap doc was written against a post-`worker_threads`-migration view that has since been partially rolled back. The deletions that landed are real; ~5 symbols remain that should follow once worker_threads re-stabilizes.
- **`ctrl.jsonl` IPC fallback (11)** — works, but ships *stronger* than spec: 1s mtime poll runs **alongside** `fs.watch`, not as a fallback. Cost is one `statSync` per second per active run. Deliberate, documented in JSDoc; flagging because reviewers may expect "fallback only on detected failure."
- **`Run.stop` SIGKILL (12) + transcript cap (14)** — both correct; verify-suite couldn't exercise the integration path due to the unit-test short-circuit, so `tests/integration/agentStopRestart.test.ts` is unverified at this commit.
- **Hot-reload stub (20)** — both stub sites replaced (recon caught a second one the gap doc missed). 40/40 zone tests pass.
- **Keyword-trigger drafting (9)** — was already shipped; this run added test coverage and one regex widening. The "chip" the matrix wanted is **not** added — recon downgraded it to a polish item without re-asking Sam. Worth confirming.

### The brutal facts
1. **The build is broken.** `tsc` errors in `sandboxWorker.ts` are blocking. The most plausible explanation is the parallel-write race ZONE_SANDBOX flagged: two write-capable agents shared a file and one's full-file write clobbered an export the other depended on. The recon's `crossZoneDeps` flagged this risk; the dispatcher launched them in parallel anyway. **Fix is two lines on the export side, but the gate is red until then.**
2. **One unit test is red.** `disableKnobs.test.ts:188` was flagged as pre-existing by two independent agents (HITL, OTEL) via stash-and-rerun, but it sits in the file `src/index.ts` (+115 LOC churn this run) — worth a second look before fully accepting "pre-existing."
3. **Integration suite never ran.** `forkFromCheckpoint`, `agentStopRestart`, `writeWorkflowKeywordTrigger` — three new integration files, zero verification on this commit. The unit-test failure short-circuits stages 2 and 3 of the test script.
4. **Five zones landed concurrently with ~5300 LOC of churn.** The review surface is large. Several of the per-zone reports note "the LSP shows stale errors but they're not mine" — this kind of cross-talk is exactly the failure mode that produces compile breaks like the one in (1).

**Recommended next steps before declaring closure:** (a) fix the two `sandboxWorker.ts` exports, (b) re-run the full test pipeline (unit + security + integration), (c) confirm or fix the `disableKnobs.test.ts:188` regression, (d) wire `pi.ui.confirm` through `workflowCmd.ts:674` so HITL becomes a full feature, (e) smoke-test OTel against a real Jaeger collector.

REPORT_WRITTEN: /Users/sam.painter/Projects/pi-workflows/docs/gap-analysis/2026-05-31-closure.md
